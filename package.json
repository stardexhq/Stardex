{
  "name": "stardex",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "description": "Open-source indexer & data API for Stellar / Soroban.",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  }
}
