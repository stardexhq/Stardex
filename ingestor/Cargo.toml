# Cargo workspace for the Stardex ingestor (the Rust "engine" wing).
# Crates: the core watcher, the decoders, the reconcile engine, and the CLI.
[workspace]
resolver = "2"
members = ["crates/core", "crates/decoders", "crates/reconcile", "crates/cli"]

[workspace.package]
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/stardexhq/stardex"

[profile.release]
opt-level = "z"
lto = true
