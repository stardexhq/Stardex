# Rust
/ingestor/target
target/

# Node / pnpm
node_modules/
dist/
*.tsbuildinfo

# Env / local
.env
.env.local

# Local-only progress tracker (never pushed)
IMPLEMENTATION.md

# Local-only research / positioning notes (never pushed)
POSITIONING.md

# Local-only deploy walkthrough (never pushed)
DEPLOY.md

# OS / editor
.DS_Store
*.log
